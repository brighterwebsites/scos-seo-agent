# Skill: content-inventory

Runs `scripts/gather_content_inventory.py` for a given site and confirms the output is valid.

## Usage

```
python scripts/gather_content_inventory.py --site <site-slug>
```

Output: `C:\Users\vanes\Desktop\seo-command-center\<site>\data\content-inventory.json`
