# schema/

Schema.org JSON-LD templates for structured data injection.

Each `.json` file in this folder is a reusable template for a Schema.org type (e.g. `Article`, `LocalBusiness`, `FAQPage`). Templates use `{{placeholder}}` tokens that scripts or Claude substitute at generation time.
